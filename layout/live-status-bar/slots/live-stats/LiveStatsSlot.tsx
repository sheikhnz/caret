/**
 * Live stats plug-in — pure engine data via LiveStatusBarStats.
 */

"use client";

import { LiveStatusBarStats } from "@/modules/typing/components/LiveStatusBarStats";

export const LiveStatsSlot = () => <LiveStatusBarStats />;
