/**
 * Live chart plug-in — trailing WPM graph synced on every keystroke.
 */

"use client";

import { LiveWpmChart } from "@/modules/typing/components/LiveWpmChart";

export const LiveChartSlot = () => <LiveWpmChart />;
