/** CSS hook on `.tp-page-shell` when the live status bar rail is active. */
export const TP_LIVE_STATUS_BAR_ATTR = "data-tp-live-status-bar";

/** Server shell host — client mount syncs drawer state onto this element. */
export const LIVE_STATUS_BAR_HOST_ID = "tp-live-status-bar-host";

/** Adaptive width bounds for the right-hand stats rail (see `styles/tokens.css`). */
export const LIVE_STATUS_BAR_MIN_WIDTH_PX = 320;
export const LIVE_STATUS_BAR_MAX_WIDTH_PX = 560;

export const LIVE_STATUS_BAR_PANEL_ID = "tp-live-status-bar-panel";
