/**
 * Outcomes from playground drawers — extend when adding new drawers.
 * Handle new types in handle-playground-drawer-action.ts.
 */

export type PlaygroundDrawerAction = { type: "customTextApplied" };
