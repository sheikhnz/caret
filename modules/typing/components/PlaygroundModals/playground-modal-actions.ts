/**
 * Outcomes from playground modals — extend when adding new modals.
 * Handle new types in handle-playground-modal-action.ts.
 */

export type PlaygroundModalAction = { type: "customTextApplied" };
